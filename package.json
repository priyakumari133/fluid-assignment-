{
  "name": "task-board",
  "version": "1.0.0",
  "description": "Full-stack Task Board Application - Combined Frontend & Backend",
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install && cd ..",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm start",
    "build": "cd client && npm run build",
    "build:all": "npm install && cd client && npm install && npm run build && cd ../server && npm install",
    "start": "npm run build:all && cd server && npm start",
    "dev": "npm run build && npm run dev:server"
  },
  "dependencies": {}
}
